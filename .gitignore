# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# live, mutable SQLite store — bootstrapped from data/reviews.seed.json
# and data/posts.seed.json at runtime; never overwrite it via git
/data/app.db
/data/app.db-wal
/data/app.db-shm

# pre-SQLite live data files, kept ignored in case they still exist locally
/data/reviews.json
/data/posts.json

# typescript
*.tsbuildinfo
next-env.d.ts

#claude
CLAUDE.md

