# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# live, mutable review store — bootstrapped from data/reviews.seed.json
# at runtime; never overwrite it via git
/data/reviews.json

# live, mutable post store — bootstrapped from data/posts.seed.json
# at runtime; never overwrite it via git
/data/posts.json

# typescript
*.tsbuildinfo
next-env.d.ts

#claude
CLAUDE.md

